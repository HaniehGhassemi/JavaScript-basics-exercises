//Get 2 number
//Raise the first number to the power of the second number
//Show result

let FirstNumber = prompt("Input first number:" , 0);
let SecondNumber = prompt("input second number:" , 0);

let PowerOfNumbers = FirstNumber ** SecondNumber;
alert( PowerOfNumbers );